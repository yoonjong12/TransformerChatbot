import os

weights_dir = os.path.normpath('./weight/')
vocab_dir = os.path.normpath('./tokenizer/')

# tensorflow tokenizer
vocab_name = "tokenizer_data.json"

# Keras model
weights_name = "my_keras_model.h5"
